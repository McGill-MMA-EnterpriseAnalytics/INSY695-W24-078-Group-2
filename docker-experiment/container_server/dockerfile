# Using the official Python Docker Image
FROM python:3.11.8-slim

# Setting up working directory within the created container
WORKDIR /app
# Copy the current directory contents into the container at /usr/src/app
COPY . /app

# Package installation via pip from requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Making port 80 available to the world outside this container for external comms
EXPOSE 80

COPY . .

# Instructions upon running the container
CMD ["uvicorn", "main-server:app", "--host", "0.0.0.0", "--port", "80"]

